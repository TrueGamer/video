<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white_color"
    android:orientation="vertical"
    >

    <ImageView
        android:id="@+id/iv_image"
        android:layout_width="match_parent"
        android:layout_height="186dp"
        android:scaleType="centerCrop"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#55000000"
        android:layout_alignRight="@id/iv_image"
        android:layout_alignLeft="@id/iv_image"
        android:layout_alignTop="@id/iv_image"
        android:layout_alignBottom="@id/iv_image"
        />

    <TextView
        android:id="@+id/tv_news_title"
        style="@style/newsItemImgOneTextTitleStyle"
        android:textColor="@color/sign_day_black_text_color"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_below="@id/iv_image"
        />

    <View
        android:layout_below="@id/tv_news_title"
        android:layout_width="match_parent"
        android:layout_height="2px"
        android:background="@color/home_item_divider_color"
        android:id="@+id/view2"/>

</RelativeLayout>
